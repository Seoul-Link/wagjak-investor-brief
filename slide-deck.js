(()=>{const slides=[...document.querySelectorAll('.deck-slide')],prev=document.getElementById('prevSlide'),next=document.getElementById('nextSlide'),counter=document.getElementById('slideCount'),progress=document.querySelector('.progress span'),modal=document.getElementById('videoModal'),player=modal?.querySelector('video'),params=new URLSearchParams(location.search);let idx=Math.min(slides.length-1,Math.max(0,(parseInt(params.get('slide')||'1',10)||1)-1));if(params.get('embedded')==='1')document.body.classList.add('embedded');function show(n,push=true){idx=Math.min(slides.length-1,Math.max(0,n));slides.forEach((s,i)=>{s.classList.toggle('active',i===idx);s.setAttribute('aria-hidden',i===idx?'false':'true');if(i===idx)s.scrollTop=0});prev.disabled=idx===0;next.disabled=idx===slides.length-1;counter.textContent=String(idx+1).padStart(2,'0')+' / '+String(slides.length).padStart(2,'0');progress.style.width=((idx+1)/slides.length*100)+'%';if(push){const u=new URL(location.href);u.searchParams.set('slide',idx+1);history.replaceState(null,'',u)}}function closeModal(){if(!modal)return;modal.classList.remove('open');modal.setAttribute('aria-hidden','true');player.pause();player.removeAttribute('src');player.load()}prev?.addEventListener('click',()=>show(idx-1));next?.addEventListener('click',()=>show(idx+1));document.addEventListener('keydown',e=>{if(modal?.classList.contains('open')){if(e.key==='Escape')closeModal();return}if(e.target instanceof Element && e.target.closest('video,a,button,input,textarea,select,[contenteditable]'))return;if(['ArrowRight','PageDown',' '].includes(e.key)){e.preventDefault();show(idx+1)}else if(['ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();show(idx-1)}else if(e.key==='Home'){e.preventDefault();show(0)}else if(e.key==='End'){e.preventDefault();show(slides.length-1)}});let start=null;document.querySelector('.slides')?.addEventListener('touchstart',e=>{if(e.touches.length===1)start={x:e.touches[0].clientX,y:e.touches[0].clientY}},{passive:true});document.querySelector('.slides')?.addEventListener('touchend',e=>{if(!start||e.changedTouches.length!==1)return;const dx=e.changedTouches[0].clientX-start.x,dy=e.changedTouches[0].clientY-start.y;if(Math.abs(dx)>55&&Math.abs(dx)>Math.abs(dy)*1.3)show(idx+(dx<0?1:-1));start=null},{passive:true});document.querySelectorAll('.video-trigger').forEach(b=>b.addEventListener('click',()=>{if(!modal)return;const src=b.dataset.src,poster=b.dataset.poster,label=b.dataset.label;player.poster=poster;player.src=src;player.controls=true;player.preload='metadata';modal.querySelector('.modal-label').textContent=label;modal.classList.add('open');modal.setAttribute('aria-hidden','false');player.play().catch(()=>{})}));modal?.querySelector('button')?.addEventListener('click',closeModal);modal?.addEventListener('click',e=>{if(e.target===modal)closeModal()});show(idx,false)})();
